<%= provide :main_menu do %> 
  <div class="bg-white shadow">
    <div class="container mx-auto px-4">
      <div class="flex items-center justify-between py-4">
        <div>
          <a href="/" class="text-gray-800 text-xl font-semibold hover:text-blue-600 mr-4">LOGO</a>
        </div>

        <div class="hidden sm:flex sm:items-center">
          <a href="#" class="text-gray-800 text-sm font-semibold hover:text-blue-600 mr-4">Products</a>
          <a href="#" class="text-gray-800 text-sm font-semibold hover:text-blue-600 mr-4">Marketplace</a>
          <a href="#" class="text-gray-800 text-sm font-semibold hover:text-blue-600 mr-4">Partners</a>
          <a href="#" class="text-gray-800 text-sm font-semibold hover:text-blue-600">Pricing</a>
        </div>

        <div class="hidden sm:flex sm:items-center">
          <% if user_signed_in? %>
            <%= link_to 'Sign out', destroy_user_session_path, data: { turbo_method: :delete }, 
                class: 'text-gray-800 text-sm font-semibold border px-4 py-2 rounded-lg hover:text-blue-600 hover:border-blue-600' %>
          <% else %>
            <%= link_to 'Sign in', new_user_session_path, class: 'text-gray-800 text-sm font-semibold hover:text-blue-600 mr-4' %>
            <%= link_to 'Sign up', new_user_registration_path, 
                class: 'text-gray-800 text-sm font-semibold border px-4 py-2 rounded-lg hover:text-blue-600 hover:border-blue-600' %>
          <% end %>
        </div>
      </div>
      
      <div class="block sm:hidden bg-white border-t-2 py-2">
        <div class="flex flex-col">
          <a href="#" class="text-gray-800 text-sm font-semibold hover:text-blue-600 mb-1">Products</a>
          <a href="#" class="text-gray-800 text-sm font-semibold hover:text-blue-600 mb-1">Marketplace</a>
          <a href="#" class="text-gray-800 text-sm font-semibold hover:text-blue-600 mb-1">Partners</a>
          <a href="#" class="text-gray-800 text-sm font-semibold hover:text-blue-600 mb-1">Pricing</a>
          <div class="flex justify-between items-center border-t-2 pt-2">
            <% if user_signed_in? %>
              <%= link_to 'Sign out', destroy_user_session_path, data: { turbo_method: :delete }, 
                class: 'text-gray-800 text-sm font-semibold border px-4 py-1 rounded-lg hover:text-blue-600 hover:border-blue-600' %>
            <% else %>
              <%= link_to 'Sign in', new_user_session_path, class: 'text-gray-800 text-sm font-semibold hover:text-blue-600 mr-4' %>
              <%= link_to 'Sign up', new_user_registration_path, class: 'text-gray-800 text-sm font-semibold border px-4 py-1 
                                                                        rounded-lg hover:text-blue-600 hover:border-blue-600' %>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
<% end%>